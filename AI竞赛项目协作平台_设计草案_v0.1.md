# 面向高校竞赛团队的 AI 项目协作平台

> 设计草案 v0.1｜当前阶段：需求与总体架构确认

## 1. 项目定位

本项目是一套面向高校竞赛团队、同时兼容软件课程项目团队的本地部署协作平台。

项目希望解决以下问题：

- 竞赛规则、需求文档、会议记录和技术资料分散，团队成员难以快速检索关键信息；
- 项目启动时任务拆分依赖个人经验，容易遗漏评分点、前置依赖和里程碑；
- 团队成员、任务、文档和讨论分别散落在多个工具中，项目进度难以统一查看；
- 普通知识库只能回答问题，不能把 AI 生成的规划真正转化为项目任务。

项目采用“传统协作平台 + AI 应用能力”的组合：

1. 先实现可独立使用的项目协作功能；
2. 再基于项目文档构建 RAG 知识库问答；
3. 使用结构化输出和工具调用生成任务规划；
4. 任务方案必须经过用户确认，才批量写入项目。

## 2. 使用范围

- 第一版仅供开发者本人和受邀请的测试用户使用；
- 系统完全在本地运行；
- GitHub 仅用于公开源码、README、架构图、部署说明、测试材料和演示视频；
- 不提供长期公网服务；
- 大模型通过互联网 API 调用，优先使用各平台免费额度，额度用完后再切换其他模型。

## 3. 用户角色

项目内采用三级角色：

### 3.1 所有者

- 修改项目基本信息；
- 邀请、移除成员；
- 设置或取消管理员；
- 管理全部任务、里程碑和文档；
- 使用 AI 任务规划；
- 删除项目。

### 3.2 管理员

- 管理任务和里程碑；
- 上传、更新、删除项目文档；
- 发起 AI 任务规划；
- 审核并批量创建 AI 生成任务；
- 查看项目统计和操作日志。

### 3.3 成员

- 查看所属项目；
- 查看任务、里程碑和文档；
- 更新自己负责的任务；
- 发表评论；
- 使用知识库问答；
- 查看项目概览。

## 4. 第一版功能范围

### 4.1 普通协作功能

- 用户登录；
- 项目创建与基本信息管理；
- 项目成员邀请与角色管理；
- 任务创建、编辑、分配、状态流转；
- 看板视图；
- 里程碑管理；
- 任务依赖关系；
- 任务评论；
- 项目文档上传与管理；
- 操作日志；
- 项目概览统计。

### 4.2 AI 能力一：项目知识库问答

支持文档类型：

- PDF；
- DOCX；
- Markdown；
- TXT。

处理流程：

```text
上传文档
→ 文件类型与大小校验
→ 保存到 MinIO
→ 创建解析任务
→ 提取文本
→ 清洗与分块
→ 调用 Embedding 模型
→ 向量写入 PostgreSQL + pgvector
→ 文档进入可检索状态
```

问答流程：

```text
用户提问
→ 校验项目成员身份
→ 对问题向量化
→ 在当前项目范围内检索文本块
→ 组合检索上下文
→ 调用聊天模型生成回答
→ 返回回答、引用文档和引用片段
```

第一版要求：

- 只能检索当前项目的文档；
- 回答必须展示引用来源；
- 没有足够依据时应明确说明无法从资料中回答；
- 删除文档时同步删除原文件、文本块和向量；
- 聊天模型可切换；
- 一个知识库建立后，Embedding 模型尽量保持不变。

### 4.3 AI 能力二：任务规划

用户可以输入项目目标，并选择部分项目文档作为规划依据。

AI 输出结构至少包括：

- 里程碑；
- 任务名称；
- 任务描述；
- 所属里程碑；
- 优先级；
- 预计工作量；
- 建议开始时间与截止时间；
- 前置任务依赖；
- 建议负责人，可为空；
- 生成依据或关联文档。

执行流程：

```text
用户输入目标并选择文档
→ AI 生成结构化任务方案
→ 后端进行 JSON Schema 与业务规则校验
→ 前端展示可编辑预览
→ 用户修改、删除或补充任务
→ 用户确认
→ 后端再次校验权限、时间和依赖关系
→ 在一个事务中批量创建里程碑、任务和依赖
→ 保存 AI 规划版本与操作日志
```

AI 不允许未经用户确认直接修改项目数据。

## 5. 总体架构

第一版采用模块化单体架构。

```text
Vue 3 前端
    │
    ▼
Spring Boot 模块化单体
    ├── 认证与用户模块
    ├── 项目与成员模块
    ├── 任务与里程碑模块
    ├── 评论模块
    ├── 文档与知识库模块
    ├── AI 问答模块
    ├── AI 任务规划模块
    ├── 统计模块
    └── 操作日志模块
        │
        ├── PostgreSQL + pgvector
        ├── Redis
        ├── MinIO
        └── 多模型 API
```

模块之间通过明确的应用服务接口交互。AI 与文档处理部分应保持边界清晰，后续可以拆分为独立服务，但第一版不进行微服务化。

## 6. 技术栈

### 6.1 后端

- Java 17 或 21；
- Spring Boot；
- Spring Security；
- Spring AI；
- MyBatis-Plus 或 Spring Data JDBC/JPA，后续确认；
- PostgreSQL；
- pgvector；
- Redis；
- MinIO；
- Maven；
- Docker Compose。

### 6.2 前端

- Vue 3；
- TypeScript；
- Element Plus；
- Pinia；
- Axios；
- Markdown 渲染；
- SSE 流式问答，可作为第一版后半程功能。

### 6.3 AI 模型接入

业务代码不直接依赖具体供应商，统一通过自定义网关调用：

```text
KnowledgeQaService       TaskPlanningService
          \                 /
           \               /
             AiModelGateway
                    │
          SpringAiModelGateway
                    │
       免费模型 / DeepSeek / 其他兼容 API
```

模型配置至少包含：

- Provider；
- Base URL；
- API Key；
- Chat Model；
- Embedding Model；
- 是否支持流式输出；
- 是否支持结构化输出；
- 是否支持 Tool Calling；
- 超时时间；
- 重试次数；
- 是否启用。

第一版由管理员手动切换模型，不实现自动额度查询和复杂智能路由。

## 7. 本地部署组成

```text
Docker Compose
├── PostgreSQL + pgvector
├── Redis
├── MinIO
└── 可选：后端与前端容器

本地开发进程
├── Spring Boot
└── Vue 3

外部依赖
└── 大模型与 Embedding API
```

API Key 使用环境变量或本地 `.env` 文件保存，不能提交到 GitHub。

## 8. 项目边界

第一版不做：

- 微服务；
- 长期公网部署；
- 开放注册；
- 实时聊天；
- 复杂日历；
- 文件版本管理；
- Excel、PPT 文档解析；
- 自动查询各模型剩余额度；
- AI 未经确认直接创建、修改或删除任务；
- 复杂多 Agent 协作；
- 模型训练和微调。

## 9. 预期简历价值

项目完成后，应能够证明：

- 能独立完成一个完整的 Java Web 产品；
- 能设计项目级 RBAC 与数据隔离；
- 能处理文件上传、对象存储和异步文档解析；
- 能使用 PostgreSQL + pgvector 构建 RAG；
- 能处理结构化输出、任务依赖和批量事务；
- 能设计多模型适配层；
- 能通过 Docker Compose 提供可复现的本地环境；
- 能对 AI 输出进行校验，而不是直接信任模型结果。

## 10. 后续待确认设计

后续将继续确认：

- 详细页面与用户操作流程；
- 任务状态与优先级；
- 模块包结构；
- 数据库核心表；
- 文档解析和分块策略；
- RAG 检索策略；
- AI 任务规划 Schema；
- 异常处理与日志；
- 测试与评测方案；
- 6～8 周开发计划。
