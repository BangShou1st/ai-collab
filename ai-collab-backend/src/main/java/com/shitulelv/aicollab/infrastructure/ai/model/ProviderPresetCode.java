package com.shitulelv.aicollab.infrastructure.ai.model;

/** V2.1 product preset. Wire protocol stays OPENAI_COMPATIBLE; never add ZEN to ModelProviderType. */
public enum ProviderPresetCode {
    OPENCODE_ZEN_FREE
}
